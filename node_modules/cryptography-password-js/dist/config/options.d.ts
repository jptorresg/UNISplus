export namespace DEFAULT_OPTIONS {
    let keyLen: number;
    let N: number;
    let r: number;
    let p: number;
    let maxmem: number;
}
export namespace RATE_LIMIT_OPTIONS {
    let maxAttempts: number;
    let windowMs: number;
}
