<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>

<body class="dashboard-page">
    
    <%- include('partials/header', {
        linkLeft: '',
        iconLeft: '',
        leftFunction: '',
        linkRight: '',
        iconRight: '',
        rightFunction: ''
    }) %>

    <main class="content">

        <div class="profile-card">
            <div 
                class="background-image"
                style="background-image: url('<%= user.background_pic ? ('/uploads/' + user.background_pic) : '/images/background_default_image.png' %>');" 
            ></div>
            <div class="profile-info">
                <img 
                    src="<%= user.profile_pic ? ('/uploads/' + user.profile_pic) : '/images/default_profile_image.jpg' %>" 
                    alt="Foto de perfil" 
                    class="profile-profile-pic"
                >
                <h2 class="profile-name"><%= user.name %></h2>
                <p class="profile-description"><%= user.description || 'Sin descripción todavía' %></p>
            </div>
        </div>        
        
        <div class="settings-container">
            <div class="settings-section">
                <h3>Configuración</h3>
                <a href="/editPerfil" style="text-decoration: none;"><div class="settings-item">
                    <i class="fa-solid fa-pen"></i>
                    <span>Editar perfil</span>
                </div></a>
                <a href="/perfil/changePassword" style="text-decoration: none;"><div class="settings-item">
                    <i class="fa-solid fa-key"></i>
                    <span>Cambiar contraseña</span>
                </div></a>
                <div class="settings-item">
                    <i class="fa-solid fa-globe"></i>
                    <span>Redes sociales</span>
                </div>
            </div>

            <form action="/logout" method="POST">
                <button type="submit" class="settings-item logout-btn">
                    <i class="fa-solid fa-arrow-right-from-bracket"></i>
                    <span>Cerrar sesión</span>
                </button>
            </form>
        </div>        

    </main>

    <%- include ('partials/footer.ejs') %>

</body>
<script src="/script.js"></script>
</html>